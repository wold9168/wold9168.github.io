---
layout: post
title: "k8s异常DNS开销"
tags: [kubernetes, DNS,]
author:
  - wold9168
math: false
render_with_liquid: false
---

遇到一个很牛逼的 DNS 开销问题。

某测试用 K8s 集群内，curl 测试时，DNS 延迟飞到了 20ms，也不清楚是什么原因：

以下的 `test-shell` 是以以下指令创设的一台设备：

```bash
kubectl --context kind-cross-test-1 run test-shell --image=debian:12 --rm -it \                                   ─╯
--restart=Never \
--image-pull-policy=Never \
-- \
  bash
```

# 问题表征

```bash
root@test-shell:/# curl -x socks5://tailscale-proxy.default.svc.cluster.local:1055 -k -s -w "DNS: %{time_namelookup}s
TCP: %{time_connect}s     
SSL: %{time_appconnect}s
TTFB: %{time_starttransfer}s
Total: %{time_total}s
                     
" https://k8sbc.default.svc.cross-test-1.remote
Hello Kubernetes bootcamp! | Running on: kubernetes-bootcamp-59689497fb-9486x | v=1
DNS: 0.024277s # 超绝异常 DNS 延迟！！
TCP: 0.024437s
SSL: 0.057949s
TTFB: 0.058804s
Total: 0.058896s
```

# 问题解决

折腾了几天，随求助于 D 指导，用`strace`手忙脚乱分析一通，D 指导忽然发散思维，指出 `/etc/resolv.conf` 可能存在问题。

遂翻阅 `resolv.conf` 送呈 D 指导：
```bash
root@test-shell:/# cat /etc/resolv.conf
search default.svc.cluster.local svc.cluster.local cluster.local openstacklocal
nameserver 10.96.0.10
options ndots:5
```

D 指导的原话是：

> 实际发生的查询顺序是：
> 
> - 先查 tailscale-proxy.default.svc.cluster.local.**default.svc.cluster.local**（拼接第一个 search 域）—— 失败（NXDOMAIN）
> - 再查 tailscale-proxy.default.svc.cluster.local.**svc.cluster.local** —— 失败
> - 再查 tailscale-proxy.default.svc.cluster.local.**cluster.local** —— 失败
> - 再查 tailscale-proxy.default.svc.cluster.local.**openstacklocal** —— 失败
> - 最后才查 tailscale-proxy.default.svc.cluster.local.（不加任何后缀的原始域名）—— 成功！
> 
> 虽然 DNS 服务器对每次失败查询都很快（约 1ms），但连续进行 4~5 轮查询的累积耗时（网络往返 + 客户端处理），就构成了您看到的 20~25ms。

```bash
root@test-shell:/# curl -x socks5://tailscale-proxy.default.svc.cluster.local.:1055 -k -s -w "DNS: %{time_namelookup}s
TCP: %{time_connect}s
SSL: %{time_appconnect}s
TTFB: %{time_starttransfer}s
Total: %{time_total}s

" https://k8sbc.default.svc.cross-test-1.remote
Hello Kubernetes bootcamp! | Running on: kubernetes-bootcamp-59689497fb-9486x | v=1
DNS: 0.001073s
TCP: 0.001269s
SSL: 0.043280s
TTFB: 0.069365s
Total: 0.069472s
```

```bash
root@test-shell:/# curl -x socks5://tailscale-proxy:1055 -k -s -w "DNS: %{time_namelookup}s
TCP: %{time_connect}s
SSL: %{time_appconnect}s
TTFB: %{time_starttransfer}s
Total: %{time_total}s

" https://k8sbc.default.svc.cross-test-1.remote
Hello Kubernetes bootcamp! | Running on: kubernetes-bootcamp-59689497fb-9486x | v=1
DNS: 0.000956s
TCP: 0.001134s
SSL: 0.030923s
TTFB: 0.031935s
Total: 0.032013s
```

颇为难绷，不曾想有一日竟然被 `resolv.conf` 击落，随发 blog 纪念。

主要是之前测试的时候同样是用 debian:12 的镜像，也没见有 1ms 以上的 DNS 开销。谁能往 `resolv.conf` 上想！

---

与 D 指导（DeepSeek）的聊天记录：https://chat.deepseek.com/share/9q9n5c9dmh29syi2sd

---

btw，我们测试的时候还发现 `alpine-bash-curl:latest` 这个镜像的 curl 访问同一个域名的延迟上天，SSL 延迟拉爆了（3.28s）。不清楚什么情况。

```bash
test-shell-alpine:/# curl -x socks5://tailscale-proxy:1055 -k -s -w "DNS: %{time_namelookup}s
TCP: %{time_connect}s
SSL: %{time_appconnect}s
TTFB: %{time_starttransfer}s
Total: %{time_total}s

" https://k8sbc.default.svc.cross-test-1.remote
Hello Kubernetes bootcamp! | Running on: kubernetes-bootcamp-59689497fb-9486x | v=1
DNS: 0.000822s
TCP: 0.000987s
SSL: 4.008348s
TTFB: 4.009349s
Total: 4.009426s
```


估计是 alpine 的 curl 有什么编译选项没开。

总之网络测试还得是用 Debian 这种老牌发行版的镜像比较好。

总之就是颇为难绷。